<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.projectors.mvc.IqnaQDAO">

	<insert id="insert">
		INSERT INTO QUESTION (QUESTION_NO, PIN_NO, TITLE, CONTENT, CREATED_DATE)
		VALUES(QUESTIONO_SEQ.NEXTVAL(), 'UP2', ${title}, ${content}, SYSDATE)
	</insert>
	
	<select id="getQuestionList" resultType="com.projectors.mvc.QnaQDTO">
		SELECT Q.QUESTION_NO, Q.TITLE, Q.CREATED_DATE, NVL2(A.CREATED_DATE,'답변완료','답변대기중') AS IS_REPLY
	 	FROM QUESTION Q JOIN ANSWER A 
		ON Q.PIN_NO = 'UP1'	 						<!-- 특정 회원의 질문만 출력. -->
		<!-- WHERE PIN_NO = #{questionPinNo} -->
	</select>

	<select id="viewQuestionDetail" resultType="com.projectors.mvc.QnaQDTO">
		SELECT QUESTION_NO, TITLE, CONTENT, CREATED_DATE
		FROM QUESTION
		WHERE PIN_NO='UP1'						<!-- 임시 정적구성 -->
		<!-- WHERE QUESTION_NO = #{questionNo} -->
	</select>
	
</mapper>